# PostgreSQL connection string (from Neon, Vercel Postgres, or any Postgres provider)
DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"

# Anthropic API key (used by extraction scripts, not the web app)
# ANTHROPIC_API_KEY="sk-ant-..."
